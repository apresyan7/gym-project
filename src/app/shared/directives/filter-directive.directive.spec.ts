import { FilterDirectiveDirective } from './filter-directive.directive';

describe('FilterDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
